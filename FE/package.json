{
  "name": "noteboard",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:docs": "vite build && mkdir -p ../docs && (cd ../docs && touch .nojekyll && cp index.html 404.html) && ls -lah ../docs",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.3.0",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.1",
    "vite": "5.4.3"
  }
}
